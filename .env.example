# Replace with your real PostgreSQL credentials. Format:
# postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE?schema=public
# - USERNAME: e.g. postgres (or a user you created)
# - PASSWORD: the password you set for that user
# - HOST: localhost if PostgreSQL is on this machine
# - PORT: usually 5432
# - DATABASE: ExoraOps (create it first: CREATE DATABASE "ExoraOps";)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/ExoraOps?schema=public"
JWT_SECRET="your-secret-at-least-32-chars-long"
NEXTAUTH_URL="http://localhost:3000"
